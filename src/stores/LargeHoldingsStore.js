import  { defineStore } from "pinia";

export const useLargeHoldingsStore = defineStore('largeHoldings', () => {
    // State == ref
    const largeHoldings = ref([]);
    const loading = ref(false);

    // Getters == computed


    // Actions == function







});
